STOP. Read fully before acting. This is a complete debugging pass, not a patch job.

Go through the ENTIRE codebase line by line. Do not skip files. Do not declare anything fixed without testing it. Work in this exact order and produce a report file for each phase.

PHASE 1 — ERROR SCAN (no fixes yet, just find):
- Run the app and capture every console error and warning
- Capture every network/API error (failed requests, 400s, 500s)
- List every file with syntax errors, broken imports, or unused/missing variables
- Output: ERRORS_FOUND.md. Show me before fixing.

PHASE 2 — BROKEN FEATURES & LINKS:
- Test every page route — does it load without error?
- Test every button — does it trigger the correct action?
- Test every form — does it submit and show success/error states?
- Test every link — does it go to the right place? Flag every dead or wrong link.
- Test every navigation path
- Output: BROKEN_ITEMS.md with each item marked WORKING or BROKEN (with reason)

PHASE 3 — WORDING & COPY:
- Find every spelling mistake, grammar error, and typo in the UI text
- Find placeholder text left in ("lorem ipsum", "TODO", "Your text here", test strings)
- Find inconsistent wording (e.g. tier names, button labels that don't match)
- Find error messages that are unclear or unprofessional
- Output: COPY_ISSUES.md listing each with the file location and suggested fix

PHASE 4 — FIX EVERYTHING FOUND:
- Fix every item from Phases 1-3
- After each fix, TEST it actually works — do not assume
- Re-run the app and confirm zero console errors remain
- Output: FIXES_APPLIED.md listing what was fixed and the test result for each

PHASE 5 — FINAL VERIFICATION:
- Confirm app builds with zero errors and zero warnings
- Confirm every page loads, every button works, every link is valid
- Output: a final pass/fail checklist

RULES:
1. Do NOT make one-line patches in endless loops. Find everything first (Phases 1-3), show me, then fix in one pass.
2. Do NOT claim something is fixed without showing the test result.
3. Do NOT touch the authentication or payment code unless it appears in the error scan — those are working and I don't want them broken.
4. If you're unsure about a fix, ask me before making destructive changes.
5. Commit to Git before you start, so nothing is lost.

Begin Phase 1 only. Show me ERRORS_FOUND.md and wait.
