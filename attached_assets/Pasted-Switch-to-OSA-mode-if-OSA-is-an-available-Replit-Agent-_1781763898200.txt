Switch to OSA mode if OSA is an available Replit Agent mode. If OSA is not available, use the strongest autonomous build-and-debug mode available and explicitly report that substitution. Do not skip, suppress, ignore, disable, or hide any error, warning, failing test, typecheck, security issue, permission issue, or build issue.

The current typecheck analysis has been running for about 52 minutes. Treat it as potentially stale. Inspect the running processes and logs, terminate only stale typecheck processes, then rerun each check separately with a bounded timeout and captured exit code. Fix root causes, not symptoms.

Work in checkpoints and do not submit a new Google Play release yet:
1. Baseline: inspect git status and preserve existing user changes. Record current package versions and build configuration.
2. Web: run the web typecheck, lint, tests, and production build. Fix every failure.
3. API: run the API typecheck, lint, tests, and production build. Fix every failure.
4. Android mobile: inspect artifacts/raimzeal-mobile app.json, eas.json, package.json, Expo config, native plugins, deep links, Supabase auth callbacks, API base URL, Stripe donation URL, export/share, camera/barcode, photo picker, notifications, exact alarms, offline behavior, and account deletion.
5. Permission audit: map every one of the 41 Play-declared Android permissions to the exact dependency and visible feature. Remove unnecessary permissions using Expo config blockedPermissions or dependency/config cleanup. Pay special attention to ACCESS_FINE_LOCATION, ACCESS_COARSE_LOCATION, ACTIVITY_RECOGNITION, RECORD_AUDIO, READ_MEDIA_AUDIO, READ_MEDIA_VIDEO, SYSTEM_ALERT_WINDOW, USE_EXACT_ALARM, RECEIVE_BOOT_COMPLETED, READ_EXTERNAL_STORAGE, and WRITE_EXTERNAL_STORAGE. Keep only permissions proven necessary. Do not break camera barcode scanning, user-selected photo access, notifications, biometric auth, or required network access.
6. Mobile validation: run npm install integrity checks, npx tsc --noEmit, npx expo-doctor, npx expo config --type public, unit/integration tests, and a clean Android preview build. Inspect the generated Android manifest and compare declared permissions against the approved minimum list.
7. Security: resolve the active Community Authorization issue and dependency vulnerabilities without weakening authorization or using broad bypasses. Verify Supabase row-level permissions for post, comment, like, edit, and delete actions.
8. Release quality: add release notes for version 1.3.0 or prepare notes for the next version; verify package com.econteur.raimzeal, API 26+, target SDK 36, 16 KB page compatibility, signing configuration, and native debug symbols.
9. Regression test login, signup, email verification, reset password, auth callback, Ovia AI, membership tiers Foundation/Rise/Reign/Legacy, Stripe donation URL https://donate.stripe.com/aFa6oH7GE50z37Xdmh6kg00, community, export, camera, photo picker, notifications, settings, privacy, terms, and account deletion.
10. Final gate: all checks must have exit code 0. Create a checkpoint after each successful phase. Produce a final table with command, exit code, result, changed files, remaining warnings, Android permission before/after list, and exact blockers. Do not claim success without evidence. Do not publish or submit to Google Play until every required check is clean and I explicitly approve submission. Never expose or regenerate secret keys.